{
  "name": "barcode-ocr-app",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "html5-qrcode": "^2.3.8",
    "jspdf": "^4.2.1",
    "jspdf-autotable": "^5.0.8",
    "lucide-react": "^1.17.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "tesseract.js": "^5.0.4"
  },
  "scripts": {
    "dev": "set GENERATE_SOURCEMAP=false&& react-scripts start",
    "build": "set GENERATE_SOURCEMAP=false&& react-scripts build",
    "deploy": "npm run build && wrangler pages deploy build --project-name barcodescanner"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
